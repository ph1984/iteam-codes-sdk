# Instruções para a IA

Este projeto usa o **iTeam Codes SDK**. Leia `AGENTS.md` — ele explica como criar,
testar e fazer deploy de Codes (SDK kv/datastore, result(), idempotência por slug,
.env nunca no Git, agendamento cron).
